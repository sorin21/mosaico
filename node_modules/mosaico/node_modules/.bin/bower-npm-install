#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../bower-npm-install/bin/bower-npm-install" "$@"
  ret=$?
else 
  node  "$basedir/../bower-npm-install/bin/bower-npm-install" "$@"
  ret=$?
fi
exit $ret
